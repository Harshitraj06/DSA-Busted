
void rotate(int arr[], int n)
{
    // Create an loop to traverse each element of the array.
    for(int i=0; i<n; i++){
    // swap the first and the last element.
       swap(arr[0],arr[i]);
    }
}
