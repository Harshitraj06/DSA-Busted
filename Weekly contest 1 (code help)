long long appleAndCoupon(int n, int m, vector<int> arr){
    sort(arr.begin(),arr.end());
    int ans=0;
    for (int i = 0; i < n; i++) {
      ans=ans+arr[i];
     }
     ans=ans-arr[n-m];
    return ans;
}
